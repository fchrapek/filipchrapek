---
import { menuLinks, siteConfig } from "@/site.config";
import { getLang } from "@/utils/i18n";

const year = new Date().getFullYear();
const lang = getLang(siteConfig.lang);
const srLabel = lang === "pl" ? "Więcej na stronie" : "More on this site";
---

<footer class="footer">
  <div class="footer__copyright">
    &copy; {siteConfig.author}
    {year}.<span class="footer__site-name"
      >&nbsp;🚀&nbsp;{siteConfig.title}</span
    >
  </div>
  <nav aria-labelledby="footer_links" class="footer__nav">
    <p id="footer_links" class="sr-only">{srLabel}</p>
    {
      menuLinks.map((link) => (
        <a class="footer__nav-link" href={link.path}>
          {link.title}
        </a>
      ))
    }
  </nav>
</footer>
